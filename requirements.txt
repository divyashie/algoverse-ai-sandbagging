# =============================================================================
# Sandbagging Pipeline v3.1 — Requirements
# =============================================================================
# Install with:
#   pip install -r requirements.txt
#
# GPU note: PyTorch is listed without a CUDA suffix here.
# If the command above installs a CPU-only torch, install it manually first:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
# then re-run pip install -r requirements.txt
#
# HuggingFace token (required for Llama and Gemma models only):
#   export HF_TOKEN=your_token_here
#
# Gemini API key (required for judge and error generation):
#   export GEMINI_API_KEY=your_key_here
# =============================================================================

# ── Core ML stack ────────────────────────────────────────────────────────────
torch>=2.1.0
transformers>=4.47.0
datasets>=2.18.0
accelerate>=0.27.0
peft>=0.13.0
trl>=0.12.0
bitsandbytes>=0.42.0          # 4-bit NF4 quantisation

# ── Numerics & stats ─────────────────────────────────────────────────────────
numpy>=1.26.0
scipy>=1.12.0                 # pearsonr for inter-rater reliability

# ── Plotting ─────────────────────────────────────────────────────────────────
matplotlib>=3.8.0
seaborn>=0.13.0               # used by visualise.py heatmap styling

# ── Gemini judge & error generation ──────────────────────────────────────────
# NOTE: google-generativeai is deprecated upstream — the new package is
# google-genai. The pipeline currently uses google-generativeai (v0.x API).
# A FutureWarning will appear at runtime but it still works.
# To silence the warning and use the supported SDK, swap to:
#   google-genai>=0.5.0
# and update the import in pipeline.py and judging.py accordingly.
google-generativeai>=0.8.0

# ── HuggingFace hub (model downloads) ────────────────────────────────────────
huggingface-hub>=0.23.0

# ── Tokeniser dependencies ────────────────────────────────────────────────────
tokenizers>=0.19.0
sentencepiece>=0.1.99         # required by some Llama and Mistral tokenisers
protobuf>=3.20.0              # required by sentencepiece

# ── Pillow (required by transformers image utils — fixes the PIL.Resampling
#    error encountered during setup) ──────────────────────────────────────────
Pillow>=9.1.0